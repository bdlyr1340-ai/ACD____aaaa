# 🆕 تحديث Pro للبوت

استبدل الملفات التالية في مجلد البوت لديك (نفس المسارات):

```
bot/services/google_account.py    ← مُحدَّث (تفعيل 2FA تلقائياً + انتظار Tap-Yes)
bot/handlers/rotate.py            ← مُحدَّث (قبول أي ملف TXT + اكتشاف تلقائي)
bot/handlers/start.py             ← مُحدَّث (قائمة احترافية)
bot/utils/keyboards.py            ← مُحدَّث (أزرار جديدة)
bot/main.py                       ← مُحدَّث (تعليق توضيحي)
```

## ✨ ما الجديد

1. **تفعيل المصادقة الثنائية تلقائياً**
   - إذا أرسلت `skip` بدل مفتاح 2FA، البوت يكتشف عدم وجود 2FA على الحساب
     ويقوم بتفعيلها لأول مرة، ثم يرسل لك المفتاح الجديد.

2. **انتظار موافقة الجوال (Tap-Yes / Device-tap)**
   - إذا طلبت Google تأكيد عبر إشعار على هاتفك، البوت ينتظر حتى
     `DEVICE_TAP_WAIT_SEC` (افتراضياً 75 ثانية).
   - إذا وافقت من الهاتف → يُكمل تلقائياً.
   - إذا رُفض الإشعار → يرسل خطأ واضح + لقطة شاشة.
   - إذا انتهت المهلة → يرسل تنبيهاً ويسترد الرصيد.

3. **استقبال أي ملف TXT** (ليس فقط أسماء معينة)
   - ارفع أي ملف نصي بأي اسم: يُعالَج تلقائياً.

4. **حساب واحد بدون أزرار**
   - الصق `email | pass | totp` مباشرة في الدردشة → يبدأ التشغيل فوراً.

5. **قائمة احترافية جديدة**: أزرار "تنسيق الإدخال"، "رفع TXT"،
   "إحصائياتي"، "ادعُ صديقاً"، إلخ.

## ⚙️ متغيرات بيئة جديدة (اختياري)

```
DEVICE_TAP_WAIT_SEC=75       # ثوانٍ انتظار موافقة الجوال
```

## ✅ خطوات التركيب

1. فك الضغط على ملفاتك القديمة (استبدال).
2. أعد تشغيل البوت — لا حاجة لتغيير قاعدة البيانات.
